# Git Flow Basico 
Comando para el flujo de git basico
```sh
git status 
git add . o git add -a
git status 
git commit -m "Descripcion del cambio en el proyecto"
git push
```
## Nota:
Si no me dice quien soy, entonces necesario restaurar la PC.